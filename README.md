# Narad-Email-Agent
Narad Email Agent lets users compose and send emails via a conversational CLI. It supports both one-line and interactive modes, works with Gmail using SMTP, and runs offline (except for sending). Powered by local LLMs, it's a secure, GUI-free email assistant.
